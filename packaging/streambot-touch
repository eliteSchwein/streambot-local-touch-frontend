#!/bin/bash
set -euo pipefail

MCCONFIGFILE="$HOME/.config/streambot/streambot-touch.cfg"

while [[ $# -gt 0 ]]; do
    case "$1" in
        --app-config|--config_path)
            MCCONFIGFILE="$2"
            shift 2
            ;;
        *)
            echo "Unknown argument: $1" >&2
            exit 1
            ;;
    esac
done

export STREAMBOT_TOUCH_CONFIG="$MCCONFIGFILE"

# Streambot Touch uses its own in-app QML keyboard.
unset QT_IM_MODULE
unset QT_IM_MODULES
unset QT_VIRTUALKEYBOARD_STYLE
unset QT_VIRTUALKEYBOARD_DESKTOP_DISABLE

exec /usr/bin/qs --path /usr/share/streambot-touch
